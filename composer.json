{
    "name": "quickpos/landing-page",
    "description": "QuickPOS - Modern POS Landing Page with CI/CD Pipeline",
    "type": "project",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0",
        "phpstan/phpstan": "^1.10"
    },
    "autoload": {
        "files": [
            "includes/validation.php"
        ]
    },
    "scripts": {
        "test": "phpunit",
        "analyse": "phpstan analyse includes/ --level=5",
        "lint": "php -l includes/validation.php && php -l includes/db.php && php -l thankyou.php"
    }
}
